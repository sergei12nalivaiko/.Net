@*@model IEnumerable<FuelProject.Models.FuelDailyStatement>*@

@{
    ViewBag.Title = "FuelStatementDateSearch";
    //Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="~/Scripts/jquery-3.3.1.js"></script>


@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumberStatement)
            </th>

        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NumberStatement)
                </td>

            </tr>
        }

    </table>*@
<table class="table table-bordered">
    <tr>

        @foreach (var item in ViewBag.FDS)
        {
            <th>
                @item
            </th>

        }

    </tr>
    <tr>
        @foreach (var item1 in ViewBag.GS)
        {
            <th>
                @item1
            </th>
        }
    </tr>
    <tr>
        @foreach (var item2 in ViewBag.GSK)
        {
            <th>
                @item2
            </th>
        }
    </tr>
</table>
<h5></h5>
<table class="table table-bordered">
    <tr>
        <th>
            <h3>Итого = @ViewBag.SummaSDV л</h3>
        </th>
    </tr>
    <tr>
        <th>
            <h3>Итого = @ViewBag.SummaSKGDV  кг</h3>
        </th>
    </tr>

</table>
